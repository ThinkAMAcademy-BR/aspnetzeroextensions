using System;
using System.ComponentModel.DataAnnotations;
using Abp.Application.Services.Dto;
using Abp.AutoMapper;

namespace ${namespace}
{
    [AutoMap(typeof())]
    public class ${classname}: FullAuditedEntityDto<Guid>
    {
        ${cursor}
    }
}